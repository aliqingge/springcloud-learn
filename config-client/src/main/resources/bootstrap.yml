spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:9999/
      profile: default
      label: master

server:
  port: 9001

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
