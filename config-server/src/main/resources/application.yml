
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aliqingge/springcloud-config-repository.git
server:
  port: 9999

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
